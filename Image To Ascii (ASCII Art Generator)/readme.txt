you need to have the image in the same folder as the program
sometimes the program might generate extra "new line" symbols, can be fixed by manually deleting them
if the program closes, you have written in a non-existent file
works best on images with good black and white contrast

settings file guide:
@#W9876543210?!abc;:+=,._ 	Palette								str
1				Palette flip (if using dark theme of text redactor)		bool
1				Sharpness (adds contrast)					bool
150				Size (size of the output; can not upscale the original image)	int